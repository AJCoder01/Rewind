# S019–S027 G1 implementation evidence

Date: 2026-07-15
Scope: S019–S027 only, on `codex/s019-s027-g1`
Mode: deterministic fixture/development mode; no Calendar, Gmail, OpenAI, OAuth, or other live external effect

## Outcome

Passed the S019–S027 non-effecting implementation packet. The shared v1 contracts, transactional PostgreSQL repository path, serialized fixture intake, trust boundaries, thin routes, MCP surface, review UI, cancellation flow, and automated G1 coverage are implemented. The next unfinished task is S028, deployed non-effecting proof.

## Requirement coverage

This packet covers the implemented fixture/repository slice of FR-01, FR-02, FR-03, FR-05, FR-06, FR-07, FR-08, FR-09, SAFE-03, SAFE-04, SAFE-08, SAFE-09, SAFE-10, NFR-02, NFR-04, NFR-06, NFR-07, NFR-08, and NFR-10. Live provider retrieval, approval, external execution, OAuth, recovery, and reset remain future-gate work.

## Versions and fixtures

- API contract: `v1`
- Initial plan contract: `initial-plan.v1`
- Golden contract fixture set: `golden-contracts.v1`
- Traceability catalog: `traceability.v1`
- Controlled fixture: `fixture-initial.v1`
- Prevention-rule contract: `prevention-rule.v1`
- Reset-plan contract: `reset-plan.v1`
- Artifact provenance fixtures: `controlled-content.v1`, `artifact-independence.v1`

## Verification commands

| Command | Result |
|---|---|
| `node --version` | Passed; `v24.14.0` (within package `>=24 <25`; repository pin remains `24.18.0`) |
| `npm.cmd ci` | Passed; 438 packages added, 0 vulnerabilities |
| `npm.cmd run lint` | Passed; ESLint exit 0 |
| `npm.cmd run typecheck` | Passed; TypeScript exit 0 |
| `npm.cmd test` | Passed; 27 files, 142 tests |
| `npm.cmd run build` | Passed; Next.js production build and all routes generated |
| `npm.cmd run test:e2e` | Passed; auth rejection, login, create/review, strict rendering, expiry, cancel/back, keyboard/reduced-motion/responsive checks |
| `npm.cmd run security:scan` | Passed; 126 files and 297 reachable-history blobs, no findings |
| `npm.cmd run verify:fake-production` | Passed; production fixture selection rejected |
| `npm.cmd run traceability:check` | Passed; 52 requirement records validated (`3 covered`, `18 partial`, `31 planned`) |
| `npm.cmd audit --audit-level=moderate` | Passed; 0 vulnerabilities |
| `git diff --check` | Passed; no whitespace errors |

The first local `npm ci` attempt was blocked by a Windows npm-cache permission error; the same clean install passed after approved elevated access to the local cache. No credentialed command was run. No disposable PostgreSQL container was available in this workspace, so no unknown database was contacted; S018 contains the prior clean disposable migration evidence.

## Safety and privacy

- No live provider command, OAuth grant, Calendar write, Gmail send, model call, seed, preflight, or reset was run.
- Fixture adapters are visibly labeled and production startup rejects fake-provider mode.
- Errors and MCP status omit provider identifiers, credentials, full prompts, attendee addresses, and complete mail bodies.
- No private credential was read, added, or committed; `.env.local` remains untracked/ignored.

## Remaining risk

S028–S030 must prove and freeze the deployed non-effecting MCP → authenticated API → PostgreSQL → dashboard slice. S020 SQL/repository behavior has unit coverage but no new live database run in this packet. Provider ownership, OAuth, model, approval, external-effect, recovery, prevention-rule activation, and reset gates remain intentionally unverified.
