# S007 Supabase provisioning evidence — 2026-07-15

## Scope and result

S007 passed. One dedicated Supabase PostgreSQL project was provisioned in South Asia (Mumbai), `ap-south-1`, and both private connection paths were validated without applying a migration or creating Rewind tables.

- Project reference: `ymln…sdrt` (redacted).
- Account MFA: enabled, confirmed manually by the project owner; no factor, QR code, or seed was shared.
- Data API: disabled, confirmed manually by the project owner. A credential-free REST probe returned the gateway's generic missing-key response and was treated as inconclusive rather than evidence; the available inspection browser was not signed in.
- PostgreSQL SSL enforcement: proven live because both configured connections negotiated TLS and an otherwise identical plaintext runtime connection was rejected with an SSL-specific error.
- External effects: none.
- Migration: not run; all ten Rewind foundation tables remained absent as required before S008.

## Private local configuration

| Check | Result |
|---|---|
| `.env.local` source | Loaded before `.env` |
| File permissions | `600` |
| Git handling | Ignored by `.gitignore`; untracked |
| Placeholders | None remained |
| Runtime/migration credentials | Present and distinct; values not displayed |
| TLS parameters | Exactly one `sslmode=require` and one `uselibpqcompat=true` on each URL |
| Production build with private environment present | Passed; 8 routes |
| Client static credential scan | 23 generated static files scanned; no database URL or password appeared |

`uselibpqcompat=true` is required by the installed `pg` 8.x connection-string parser so `sslmode=require` follows standard libpq encryption semantics. The initial live probe without that parameter failed during certificate-chain verification before authentication; the corrected URLs passed.

## Live connection and authorization checks

| Check | Result |
|---|---|
| Runtime mode | Shared transaction pooler, port `6543` |
| Runtime identity/database | `rewind_app` / `postgres` |
| Runtime TLS | Active |
| Runtime schema privileges | `USAGE` allowed; `CREATE` denied |
| Runtime elevated flags | Not superuser; cannot create databases/roles, inherit, replicate, or bypass RLS |
| Runtime connection limit | `10` |
| Migration mode | Shared session pooler, port `5432`; selected because the direct IPv6 endpoint was unreachable from the local network |
| Migration identity/database | `postgres` / `postgres` |
| Migration TLS | Active |
| Plaintext runtime connection | Rejected with an SSL-specific error |
| Future table ACL | `rewind_app=arwd`; API roles absent |
| Future sequence ACL | `rewind_app=rU`; API roles absent |
| Future function ACL | Owner only; `rewind_app` and API roles absent |

## S007 exit decision

All database-side and local-secret S007 acceptance checks passed. Dashboard-only MFA and Data API settings are recorded as manual owner confirmations, not inferred from network behavior. The next task is S008: apply the real migration, prove repeatability, inspect every table and constraint, verify grants on created objects, test uniqueness, and add readiness checks.
