# P0-A2 foundation decision evidence — 2026-07-14

## Scope

- Base pulled commit: `cdcb7b0`
- Execution model: this historical foundation evidence is now tracked under the unified sequential plan; it creates no person-specific task assignment
- Decision records: `OPEN-002`–`OPEN-009`, `OPEN-012`, and `OPEN-013`
- External effects: none; Calendar, Gmail, OpenAI, and live database integrations remained disabled

## Selected foundation

- Node.js 24 LTS; local pin `24.18.0`
- Vercel Functions with Fluid Compute in Mumbai (`bom1`)
- Supabase Postgres in Mumbai (`ap-south-1`)
- Signed single-operator dashboard session with same-origin mutation checks
- External/Testing Google OAuth with one selected test identity
- One team-controlled UK recipient and one team-controlled US recipient, stored outside Git
- Demo date `2026-08-20`
- Sanitized Markdown evidence in this directory; private provider receipts outside Git

## Sanitized verification

All executable checks used the pinned Node.js `v24.18.0` runtime.

| Check | Result |
|---|---|
| ESLint | Passed |
| TypeScript `--noEmit` | Passed after Next.js generated types were complete |
| Vitest 3.2.7 | 4 files, 12 tests passed |
| Next.js production build | Passed; 8 routes generated/compiled |
| Critical browser flow | Passed: unauthenticated redirect, login, World PR creation, persisted review rendering |
| Full npm dependency audit | 0 known vulnerabilities after upgrading Vitest from 3.1.4 to 3.2.7 |
| Focused repository secret-pattern scan | Passed; only `.env.example` is tracked and no credential-shaped values were found |
| Deployment configuration parse | `vercel.json` is valid JSON |

The first parallel typecheck overlapped the production build while `.next/types` was being replaced and failed on missing generated files. The required sequential rerun passed; this is recorded to avoid presenting the transient race as a code result.

## Remaining gate work

- Vercel and Supabase accounts require an authenticated user session before the selected projects can be created.
- `DATABASE_MIGRATION_URL` and `DATABASE_URL` are not configured, so a real PostgreSQL migration has not been run.
- Google OAuth client credentials, expected OIDC subject, Calendar ID, encrypted refresh token, and structured recipient mapping are not configured.
- These are explicit provisioning/G2 proof items. They do not permit fixture results to be presented as live-provider evidence.
